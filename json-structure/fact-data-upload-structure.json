{
  "$schema": "https://json-structure.org/schema/v0",
  "name": "FactDataUpload",
  "type": "object",
  "description": "Payload for uploading fact data in a Zluri sync.",
  "properties": {
    "entity": {"type": "string"},
    "page": {"type": "integer", "minimum": 1},
    "data": {"type": "array", "maxItems": 1000, "items": {"type": "object"}}
  },
  "required": ["entity", "page", "data"]
}
